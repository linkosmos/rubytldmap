language: ruby

bundler_args: --without development

rvm:
  - 2.0.0
  - 2.1.0
  - ruby-head
  - jruby-head
  - rbx

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-head
    - rvm: rbx

branches:
  only:
    - master
